# Network egress allowlist for hardened computer agent
# Format: host:port (or host for all ports on that host)
# Lines starting with # are comments. Blank lines are ignored.
# DNS resolution happens at rule-apply time — use IPs where possible for
# stability (CDN IP rotation can break hostname-based rules).

# Package registries
registry.npmjs.org:443
pypi.org:443
files.pythonhosted.org:443

# Git hosting
github.com:443
github.com:22

# Browser automation (Playwright CDN for Chromium downloads)
# Required if the computer agent uses the browser tool (hardened architecture)
# playwright.azureedge.net:443
# storage.googleapis.com:443

# Container registries (uncomment if agent pulls images)
# ghcr.io:443
# registry.hub.docker.com:443

# LLM API providers (uncomment if agent makes direct API calls)
# api.anthropic.com:443
# api.openai.com:443

# Custom: add your services below
