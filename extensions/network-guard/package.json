{
  "name": "openclaw-network-guard",
  "version": "0.1.0",
  "description": "OpenClaw plugin: application-level domain allowlisting for web_fetch and exec tool calls",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/picomatch": "^3",
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "picomatch": "^4"
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  }
}
