{
  "name": "command-guard",
  "version": "0.1.0",
  "description": "OpenClaw plugin: regex-based dangerous command blocking for exec/bash tool calls",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  }
}
