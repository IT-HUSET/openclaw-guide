{
  "name": "channel-guard",
  "version": "0.1.0",
  "description": "OpenClaw plugin: scan incoming channel messages for prompt injection using local DeBERTa ONNX model",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "@huggingface/transformers": "^3"
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  }
}
